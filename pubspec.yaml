name: video_window_workspace
description: Video Window Marketplace - Workspace Root
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.5.0 <4.0.0'

# Workspace root - packages managed via path dependencies
dependencies:

dev_dependencies:

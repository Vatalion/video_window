# Video Window - Local Development Environment Variables
# Copy this file to .env and update with your local values
# Note: .env is git-ignored for security

# ============================================================================
# POSTGRESQL CONFIGURATION (Development)
# ============================================================================
# PostgreSQL connection settings for local development
# Default port 8090 avoids conflicts with standard PostgreSQL (5432)

POSTGRES_USER=postgres
POSTGRES_DB=video_window
POSTGRES_PASSWORD=your_secure_postgres_password_here
POSTGRES_PORT=8090

# ============================================================================
# REDIS CONFIGURATION (Development)
# ============================================================================
# Redis connection settings for local development
# Default port 8091 avoids conflicts with standard Redis (6379)

REDIS_PASSWORD=your_secure_redis_password_here
REDIS_PORT=8091

# ============================================================================
# POSTGRESQL CONFIGURATION (Testing)
# ============================================================================
# Separate PostgreSQL instance for running tests
# Prevents test data from polluting development database

POSTGRES_TEST_USER=postgres
POSTGRES_TEST_DB=video_window_test
POSTGRES_TEST_PASSWORD=your_secure_test_postgres_password_here
POSTGRES_TEST_PORT=9090

# ============================================================================
# REDIS CONFIGURATION (Testing)
# ============================================================================
# Separate Redis instance for running tests

REDIS_TEST_PASSWORD=your_secure_test_redis_password_here
REDIS_TEST_PORT=9091

# ============================================================================
# SERVERPOD CONFIGURATION
# ============================================================================
# Service secret for inter-server communication
# Generate using: openssl rand -base64 32

SERVICE_SECRET=your_secure_service_secret_here

# ============================================================================
# SETUP INSTRUCTIONS
# ============================================================================
# 1. Copy this file: cp .env.example .env
# 2. Generate secure passwords:
#    - macOS/Linux: openssl rand -base64 32
#    - Windows: Use a password generator
# 3. Update all password fields above
# 4. Keep .env file secure and never commit to version control
# 5. Start services: docker compose up -d
# 6. Verify connectivity: docker compose ps

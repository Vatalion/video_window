name: story
description: Story detail page feature for Video Window
publish_to: 'none'
resolution: workspace
version: 1.0.0+1

environment:
  sdk: '>=3.5.0 <4.0.0'
  flutter: '>=3.24.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_bloc: ^8.1.3
  equatable: ^2.0.5
  video_player: ^2.8.1
  chewie: ^1.7.0
  share_plus: ^7.2.0
  url_launcher: ^6.2.5
  wakelock_plus: ^1.2.5
  connectivity_plus: ^6.1.0
  path_provider: ^2.1.1
  path: ^1.8.3
  core:
    path: ../../core
  shared:
    path: ../../shared

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
  bloc_test: ^9.1.7
  mocktail: ^1.0.4

  features: any
  video_window_flutter: any
flutter:
  generate: true

